# ============================================
# JamWatHQ Server Configuration Template
# ============================================
# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual credentials
# 3. NEVER commit the .env file to Git
# ============================================

# Server Configuration
PORT=3000
NODE_ENV=development

# Base URL (IMPORTANT: Change to your production domain)
# Development: http://localhost:3000
# Production: https://yourdomain.com
BASE_URL=http://localhost:3000

# Session Secret (CRITICAL SECURITY)
# Generate a secure random secret using one of these methods:
# Node.js: require('crypto').randomBytes(32).toString('hex')
# OpenSSL: openssl rand -hex 32
# Online: https://randomkeygen.com/ (CodeIgniter Encryption Keys)
SESSION_SECRET=GENERATE_A_SECURE_RANDOM_SECRET_HERE_32_CHARS_MINIMUM

# MongoDB Configuration
# For local development:
MONGODB_URI=mongodb://localhost:27017/jamwathq

# For MongoDB Atlas (cloud production):
# MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/jamwathq?retryWrites=true&w=majority

# Google OAuth 2.0 Credentials
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret-here
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# Facebook OAuth Credentials
# Get these from: https://developers.facebook.com/apps/
FACEBOOK_APP_ID=your-facebook-app-id-here
FACEBOOK_APP_SECRET=your-facebook-app-secret-here
FACEBOOK_CALLBACK_URL=http://localhost:3000/auth/facebook/callback

# Client URL (for CORS)
CLIENT_URL=http://localhost:3000,http://localhost:8000,http://127.0.0.1:8000

# Allow HTTP in development only (MUST be false in production)
ALLOW_INSECURE_HTTP=true

# Email Configuration
EMAIL_USER=your-email@outlook.com
EMAIL_PASS=your-app-specific-password-here
EMAIL_SERVICE=outlook
ADMIN_EMAILS=admin@yourdomain.com
